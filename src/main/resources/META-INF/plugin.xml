<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.correlationidpropagationcompanion</id>
    <name>Correlation ID Propagation Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Gutter warning icon on any Java/Kotlin Spring MVC handler
        method that receives a correlation/trace-id header via
        <code>@RequestHeader</code> and makes an outbound HTTP call in
        its own body, but never references that header parameter again
        anywhere in the body -- the common footgun where the incoming
        id is captured and then silently dropped instead of being
        forwarded to the downstream call, breaking distributed tracing
        and making cross-service log correlation impossible for that
        request.</p>
        <p><b>100% static text/PSI analysis</b> -- matches the header
        parameter name (<code>correlation-id</code>, <code>trace-id</code>,
        <code>request-id</code>, etc.) and a broad set of outbound-HTTP
        call signals (<code>RestTemplate</code>, <code>WebClient</code>,
        <code>OkHttpClient</code>, <code>.exchange(</code>, and similar)
        by simple text, so it works whether the real HTTP client
        libraries are on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only checks whether the header
        parameter's identifier is referenced again anywhere in the same
        method body -- it can't confirm the id is forwarded correctly to
        the *specific* outbound call (e.g. as the right header on the
        right request), only that it isn't obviously dropped. Calls made
        from a different method than the one receiving the header aren't
        covered.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.correlationidpropagationcompanion.gutter.DroppedCorrelationIdLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.correlationidpropagationcompanion.gutter.DroppedCorrelationIdLineMarkerProvider"/>
        <notificationGroup id="Correlation ID Propagation Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
